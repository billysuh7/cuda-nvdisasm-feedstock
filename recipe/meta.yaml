{% set name = "cuda-nvdisasm" %}
{% set version = "13.2.86" %}
{% set cuda_version = "13.2" %}
{% set platform = "linux-x86_64" %}  # [linux64]
{% set platform = "linux-ppc64le" %}  # [ppc64le]
{% set platform = "linux-sbsa" %}  # [aarch64]
{% set platform = "windows-x86_64" %}  # [win]
{% set extension = "tar.xz" %}  # [not win]
{% set extension = "zip" %}  # [win]

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://developer.download.nvidia.com/compute/cuda/redist/cuda_nvdisasm/{{ platform }}/cuda_nvdisasm-{{ platform }}-{{ version }}-archive.{{ extension }}
  sha256: a920f95954b031c2437b0888e92e0d52f78ee84bf83ac47cfa9e28cb3de7a69c  # [linux64]
  sha256: 70da552fa1ac4d96d0e37954b2a27bc9c9a1dbfb460c7de6a9a467c003b4015e  # [aarch64]
  sha256: 544b89805e40a67dd6037b0985705519efadbdd61c7a225ce13c5cc94ec3f788  # [win]

build:
  number: 0
  binary_relocation: false
  skip: true  # [osx or ppc64le]
  script:
    - move bin\* %LIBRARY_BIN%  # [win]

requirements:
  build:
    - {{ compiler("c") }}
    - {{ compiler("cxx") }}
    - {{ stdlib("c") }}
    - arm-variant * {{ arm_variant_type }}  # [aarch64]
    - cf-nvidia-tools 1  # [linux]
  host:
    - cuda-version {{ cuda_version }}
  run:
    - {{ pin_compatible("cuda-version", max_pin="x.x") }}
  run_constrained:
    - arm-variant * {{ arm_variant_type }}  # [aarch64]

test:
  requires:
    - patchelf                                        # [linux]
  files:
    - test-rpath.sh                                   # [linux]
  commands:
    - test -f $PREFIX/bin/nvdisasm                    # [linux]
    - bash test-rpath.sh                              # [linux]
    - if not exist %LIBRARY_BIN%\nvdisasm.exe exit 1  # [win]

about:
  home: https://developer.nvidia.com/cuda-toolkit
  license_file: LICENSE
  license: LicenseRef-NVIDIA-End-User-License-Agreement
  license_url: https://docs.nvidia.com/cuda/eula/index.html
  summary: nvdisasm extracts information from standalone cubin files
  description: |
    nvdisasm extracts information from standalone cubin files and presents them in
    human readable format.
  doc_url: https://docs.nvidia.com/cuda/cuda-binary-utilities/

extra:
  recipe-maintainers:
    - conda-forge/cuda
